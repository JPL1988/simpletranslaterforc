#include <stdio.h>
#include<stdlib.h>
int main{
	int i;
	int j;
	float v;
	float x;
	float[100] a;
	while(true){
		int m;
		do i=i+1;
		while (a[i]<v);
		int l;
		do j =j-1;
		while(a[j]>v);
		if(i>=j)
			break;
		x = a[i];
		a[i] = a[j];
		a[j] = x;
	}
}